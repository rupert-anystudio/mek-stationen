const langs = [
  {
    key: 'de',
    label: 'Deutsch',
  },
  {
    key: 'en',
    label: 'English',
  },
]

export const langKeys = langs.map(l => l.key)
export const langLabels = langs.map(l => l.label)

export default langs